---
title: "Inviting and managing residents"
description: "Bring residents onto PropertyPro and keep the roster current as owners, tenants, and staff change."
category: "residents"
slug: "inviting-and-managing-residents"
roles:
  - board_member
  - board_president
  - cam
  - site_manager
  - property_manager_admin
keywords:
  - residents
  - invite
  - roster
  - add
  - roles
  - offboard
  - bulk invite
  - csv import
  - directory
  - role change
tags:
  - residents
  - admin
updatedAt: "2026-06-23"
lastReviewedAt: "2026-06-23"
relatedArticles:
  - approving-join-requests
featured: false
contextPaths:
  - /dashboard/residents
  - /dashboard/import-residents
  - /communities/*/residents
---

The resident roster is the source of truth for who can see what in your community. Keeping it clean speeds up everything from announcements to violation handling.

## Invitation vs. self-service

Two paths add residents:

- **Invitation** — you send an invite; resident clicks the link and lands ready to go.
- **Self-service join** — resident submits a request; you approve or deny.

Invitations are faster and ensure unit numbers are correct from the start. Self-service is useful for communities where you can't easily get resident emails in advance.

## Sending an invitation

<StepByStep>
<Step title="Open Residents">
From the sidebar, click Residents (or `/dashboard/residents`).
</Step>

<Step title="Click Add Resident">
Enter the resident's **Full name** (required), **Email**, **Phone**, **Role** (Owner or Tenant), and **Unit ID** (the unit's numeric identifier, required). Board and manager access is granted separately from **Settings → Roles & Access** by the root manager. Keep **Send invitation email** checked to email them a sign-up link pre-filled with their unit and role.
</Step>

<Step title="Review and send">
The resident receives an email invitation with a direct sign-up link.
</Step>

<Step title="Resend if needed">
Each resident row has a **Send Invite** button to resend the invitation. There is no Sent/Accepted/Expired status tracking in the list.
</Step>
</StepByStep>

## Bulk invitations

For onboarding at scale (new communities, quarterly new-owner intake):

1. Download the CSV template from the Import page (`name`, `email`, `role`, `unit_number` columns; roles are Owner or Tenant only).
2. Fill in each row.
3. Upload. PropertyPro validates the file and creates resident records.
4. Review the summary — any rows that failed validation show the reason.
5. Send invitations afterward using **Send Invite** on each resident row — import does not bulk-email invitations.

## Role changes and offboarding

As ownership, leases, and assignments change, update the roster:

- **Tenant moves in** — add them as a Tenant linked to the unit with **Add Resident**.
- **Board / manager changes** — the root manager assigns board designations and property-manager access from **Settings → Roles & Access** (root manager only; board options are hidden for apartment communities). Changes take effect on the resident's next page load.

<Callout type="warning" title="Removing access goes through support">
Removing an owner or tenant from a community is not yet available in the Residents UI. Contact PropertyPro support to revoke a resident's access — their account stays intact for any other communities they belong to. Keeping the record rather than erasing it preserves audit continuity for payments, maintenance, and violations.
</Callout>

## Merging duplicate residents

If a resident has two accounts (old email, new email), contact PropertyPro support. There is no automatic merge and no self-service email-change or removal flow in the UI.

## Audit trail

Resident invitations and roster changes are captured in the compliance audit log. If a resident later disputes access or role assignment, you have a timestamped record.
