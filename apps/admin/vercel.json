{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "ignoreCommand": "if [ \"$VERCEL_ENV\" = \"production\" ]; then exit 0; fi; git diff --quiet HEAD^ HEAD -- 'apps/admin/' 'packages/' 'pnpm-lock.yaml' 'turbo.json' && exit 0 || exit 1",
  "buildCommand": "pnpm --filter '@propertypro/shared' --filter '@propertypro/db' --filter '@propertypro/ui' --filter '@propertypro/tokens' build && pnpm exec turbo run build --filter=@propertypro/admin",
  "installCommand": "pnpm install --frozen-lockfile",
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        {
          "key": "Strict-Transport-Security",
          "value": "max-age=63072000; includeSubDomains; preload"
        }
      ]
    }
  ]
}
