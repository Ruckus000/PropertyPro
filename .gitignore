# OS files
.DS_Store
Thumbs.db
_tmp_*
*.textClipping

# IDE
.idea/
.vscode/
*.swp
*.swo

# Dependencies
node_modules/

# Build outputs
dist/
build/
.next/
.turbo/
*.tsbuildinfo
apps/web/public/pdfjs/

# Environment files
.env
.env.local

# Test coverage
coverage/
playwright-report/
test-results/
blob-report/

# Lock files (macOS)
docs/.~lock.*
docs/*.tmp
.vercel

# Load test output (may contain session tokens)
load-test-summary*.json

# Agent run logs
.claude-ralph-runs/
.claude/worktrees/

# Agent/editor tool scratch dirs
.agents/
.claire/
.cursor/
.kiro/
.codex/
.codex-*/
.knee/
.claude/patches/
.claude/plans/
# Ignore skill contents (not the dir itself) so the one committed project
# skill below can be re-included — git can't un-ignore a file whose parent
# DIR is ignored, but it can when only the dir's contents are.
.claude/skills/*
!.claude/skills/drain-loop.md
.claude/start-*.sh
.claude/scheduled_tasks.lock
.superpowers/
skills-lock.json

# Local scratch
tmp/

# Local Supabase stack, from `supabase init` in a worktree when verifying UI
# that needs Auth + Storage (the disposable test Postgres is not enough).
# Machine-specific: ports get shifted per worktree to let stacks coexist.
supabase/

# Stray screenshots dropped at the repo root by UI/design sessions.
# Root-scoped (leading slash) so committed images under .claude/artifacts/
# and apps/ are unaffected.
/*.png
/*.jpg
/*.jpeg
