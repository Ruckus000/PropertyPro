# Design system handoff — draft

No new production tokens are proposed. The mockup references the existing semantic intent and route-scoped Florida Modern language; a production implementation would require a formal token handoff.
