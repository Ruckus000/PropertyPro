- For bulk email operations in `packages/email`, implement provider-native batch APIs (`resend.batch.send`) before falling back to sliding-window concurrency utilities (`p-map`). The `sendBulkEmail` helper encapsulates the batch logic and correctly correlates provider responses with individual recipients for status tracking.
- Do not introduce new dependencies or custom concurrency utilities (like custom `p-map` clones) into `packages/email` unless provider limits enforce it, to adhere to strict `package.json` stability requirements.
