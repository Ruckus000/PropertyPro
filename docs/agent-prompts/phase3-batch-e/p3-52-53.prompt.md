You are implementing Phase 3 Batch E in PropertyPro: P3-52 (Contract & Vendor Tracking) and P3-53 (Audit Trail Viewer).

Read these files first, in order:
1) AGENTS.md
2) PHASE3_EXECUTION_PLAN.md (Batch E section + Phase 3 invariants checklist)
3) specs/phase-3-pm-mobile/52-contract-vendor-tracking.md
4) specs/phase-3-pm-mobile/53-audit-trail-viewer.md
5) packages/shared/src/features/types.ts
6) packages/shared/src/features/community-features.ts
7) packages/shared/src/features/get-features.ts
8) packages/db/src/scoped-client.ts
9) packages/db/src/schema/compliance-audit-log.ts
10) packages/db/src/schema/enums.ts
11) packages/db/src/schema/documents.ts
12) packages/db/src/schema/user-roles.ts
13) apps/web/src/lib/api/error-handler.ts
14) apps/web/src/lib/middleware/audit-middleware.ts
15) apps/web/src/lib/api/auth.ts
16) apps/web/src/lib/api/community-membership.ts
17) apps/web/src/lib/api/tenant-context.ts
18) apps/web/src/app/api/v1/announcements/route.ts (route pattern: withErrorHandler + scoped client + audit)
19) apps/web/src/app/api/v1/pm/branding/route.ts (Phase 3 API pattern + validation + file handling)
20) apps/web/src/app/(authenticated)/pm/settings/branding/page.tsx (Phase 3 page auth/redirect pattern)
21) apps/web/src/app/(authenticated)/dashboard/page.tsx (feature-gated routing pattern)
22) apps/web/src/lib/utils/csv-validator.ts (CSV utility style)
23) apps/web/__tests__/leases/route.test.ts (feature gate + audit + route unit test style)
24) apps/web/__tests__/integration/feature-flag-enforcement.integration.test.ts (integration gate pattern)

Batch E requirements you MUST satisfy (PHASE3_EXECUTION_PLAN):
- Contract features gated off for apartment communities.
- Bid embargo respected until close date.
- Audit CSV export sanitizes formula-injection vectors.

Phase 3 invariants you MUST satisfy:
- All tenant-scoped data access uses createScopedClient(communityId) unless a documented unsafe helper exception applies.
- Every API route uses withErrorHandler.
- Every compliance-relevant mutation logs via logAuditEvent (or withAuditLog helper).
- No any and no @ts-ignore.
- Query-layer access control is enforced for tenant and role boundaries.
- New runtime code passes pnpm guard:db-access.

IMPORTANT SOURCE-OF-TRUTH RULES
- If spec file paths conflict with current repo structure, follow current repo structure and note the divergence in comments/commit message.
- Use feature flags (getFeaturesForCommunity / features.hasCompliance) instead of direct communityType comparisons for contracts gating.
- For audit viewer authorization, treat "admin users" as canonical community admin roles:
  board_member, board_president, cam, site_manager, property_manager_admin.
  (Do not allow owner/tenant.)

WHAT TO BUILD (BATCH E)

P3-52 — Contract & Vendor Tracking (condo/HOA only)
- Contract and bid data model with tenant scoping.
- Community admin UI to create/list contracts and track bids.
- Contract linked to uploaded document (documents.id FK).
- Optional link to compliance checklist item (compliance_checklist_items.id FK).
- Expiration alerts for 30/60/90-day windows (service + UI summary).
- Bid embargo: bid details must remain hidden until bidding close date.
- Conflict-of-interest flag field on contracts.
- Audit logging for contract and bid mutations.

P3-53 — Audit Trail Viewer (read-only)
- Community admin read-only audit trail page for compliance_audit_log entries.
- Filters: action type, date range, user.
- Chronological list with metadata summary.
- Cursor-based pagination (stable sort).
- CSV export endpoint/response with formula-injection sanitization.
- Non-admin users denied.

IMPLEMENTATION DECISIONS (USE THESE DEFAULTS)

1) Routes / pages (current repo conventions)
- Use authenticated pages with communityId query param (same pattern as /dashboard and PM branding pages):
  - apps/web/src/app/(authenticated)/contracts/page.tsx
  - apps/web/src/app/(authenticated)/audit-trail/page.tsx
- Add API routes:
  - apps/web/src/app/api/v1/contracts/route.ts (GET/POST/PATCH; optional DELETE if you support soft-delete)
  - apps/web/src/app/api/v1/audit-trail/route.ts (GET list + CSV export via query param format=csv)

2) DB schema (Drizzle + migrations)
- Add new tables for contracts and bids (tenant-scoped, soft-deletable):
  - contracts
  - contract_bids
- Add schema files under packages/db/src/schema/ and export them via schema/index.ts + packages/db/src/index.ts types.
- Use dates stored in UTC (AGENTS.md) and date-fns for threshold math.
- Contract value and bid amount should use numeric(..., scale: 2) following existing rentAmount conventions.
- Include createdAt / updatedAt / deletedAt columns for scoped-client compatibility.
- Link contracts.documentId -> documents.id (restrict on delete preferred).
- Link contracts.complianceChecklistItemId -> compliance_checklist_items.id (nullable).
- Link contract_bids.contractId -> contracts.id.
- Keep Drizzle migration files and meta journal/snapshot in sync (AGENTS.md learnings).

3) Access control and feature gates
- Contracts feature:
  - Require authenticated user + community membership.
  - Require community admin role (not owner/tenant).
  - Require features.hasCompliance = true (reject apartments).
- Audit trail viewer:
  - Require authenticated user + community membership.
  - Require community admin role.
  - Read-only only; no update/delete operations on compliance_audit_log.
- Enforce all access control in API/query layer, not UI-only.

4) Bid embargo behavior (strict)
- If contract.biddingClosesAt is in the future:
  - Hide bid detail rows/amounts/vendor names in API response and UI.
  - Return only a sealed/embargoed summary (e.g., count + close date).
- If biddingClosesAt is null, treat bids as visible immediately.
- If biddingClosesAt <= now, return bid details normally.
- This rule must be enforced server-side, not just in UI rendering.

5) Audit trail pagination + export
- Use cursor-based pagination ordered by (createdAt DESC, id DESC) for stability.
- Cursor payload should include both createdAt and id (base64 JSON or equivalent opaque string).
- Filter support:
  - action (exact)
  - performedBy / userId (exact userId; UI may expose user selector)
  - startDate / endDate (inclusive date range; convert to UTC boundaries)
- CSV export:
  - Reuse/create apps/web/src/lib/services/csv-export.ts
  - Sanitize formula-injection vectors per-cell by prefixing dangerous leading chars (=, +, -, @, tab) with apostrophe.
  - Escape/quote CSV fields correctly.
  - Include headers and all visible export columns.
  - Do not include raw secrets in metadata export.

6) Audit log table usage
- compliance_audit_log is append-only and exempt from soft delete filtering.
- NEVER update/delete compliance_audit_log.
- Reads should still use createScopedClient(communityId) so community_id filter is auto-applied.

7) User lookup for audit viewer (important safety/perf detail)
- Do NOT load all users globally.
- Query scoped user_roles first to get userIds for the community, then fetch only those users (filtered by IDs) for display mapping.
- Keep filtering/query logic tenant-safe and bounded.

FILES TO CREATE / UPDATE (MINIMUM EXPECTED)

Database / schema / migrations
- packages/db/src/schema/contracts.ts
- packages/db/src/schema/contract-bids.ts
- packages/db/src/schema/enums.ts (if adding contract enums)
- packages/db/src/schema/index.ts
- packages/db/src/index.ts
- packages/db/migrations/* (Drizzle migration for new tables/enum(s))
- packages/db/migrations/meta/_journal.json (if updated by generation flow)
- packages/db/migrations/meta/*_snapshot.json (if updated by generation flow)

API / services / data access
- apps/web/src/app/api/v1/contracts/route.ts
- apps/web/src/app/api/v1/audit-trail/route.ts
- apps/web/src/lib/api/contracts.ts
- apps/web/src/lib/api/audit-trail.ts
- apps/web/src/lib/services/contract-renewal-alerts.ts
- apps/web/src/lib/services/csv-export.ts

UI pages / components
- apps/web/src/app/(authenticated)/contracts/page.tsx
- apps/web/src/app/(authenticated)/audit-trail/page.tsx
- apps/web/src/components/contracts/ContractForm.tsx
- apps/web/src/components/contracts/ContractTable.tsx
- apps/web/src/components/contracts/BidTracker.tsx
- apps/web/src/components/audit/AuditTrailViewer.tsx
- apps/web/src/components/audit/AuditFilters.tsx
- apps/web/src/components/audit/AuditEntry.tsx

Tests (must add)
- apps/web/__tests__/contracts/route.test.ts
- apps/web/__tests__/audit/audit-trail-route.test.ts
- apps/web/__tests__/audit/audit-trail-viewer.test.tsx
- apps/web/__tests__/contracts/contract-table.test.tsx
- apps/web/__tests__/services/csv-export.test.ts
- Update/add integration coverage for feature gating and tenant isolation as needed

TESTING REQUIREMENTS (MUST PASS)

Contracts / bids
- Condo/HOA allowed; apartment blocked via feature flags (hasCompliance=false).
- Contract create validates required fields and linked document belongs to same community.
- Contract optional compliance checklist item must belong to same community.
- Expiration alert thresholds classify 30/60/90 days correctly (date-fns; UTC-safe).
- Bid embargo hides bid details before close date and reveals after close date.
- Contract/bid mutations write audit events.

Audit trail viewer
- Non-admin users receive 403.
- Results are reverse chronological by (createdAt, id).
- Filters (action/date/user) narrow results correctly.
- Cursor pagination returns stable next page without duplicates/skips.
- CSV export returns valid CSV and sanitizes formula injection (cells starting with =,+,-,@,tab).
- Audit viewer remains read-only (no mutation routes).

Integration / safety
- Cross-tenant access attempts fail (contracts + audit trail endpoints).
- Query-layer access control enforced (not UI-only).
- No raw db imports in runtime app code outside approved unsafe surface.

IMPLEMENTATION NOTES / EDGE CASES
- Use withErrorHandler on every route export.
- Prefer withAuditLog for contract mutation routes so request IDs are attached.
- If you need a filter helper not exported by @propertypro/db/filters (e.g., lt for cursor pagination), make a minimal typed re-export in packages/db/src/filters.ts instead of bypassing the DB abstraction.
- Keep P3-PRE-02 compatibility mindset: additive migrations only, no destructive changes to existing tables.
- Do not introduce apartment contract features; gate by CommunityFeatures only.
- Do not leak bid data before embargo expiry in API, CSV, or UI.
- Do not leak raw audit metadata that contains secrets/tokens (redact or omit sensitive keys).

VERIFICATION COMMANDS
1) pnpm --filter @propertypro/db db:migrate
2) pnpm guard:db-access
3) pnpm --filter @propertypro/web typecheck
4) pnpm exec vitest run apps/web/__tests__/contracts apps/web/__tests__/audit apps/web/__tests__/services/csv-export.test.ts
5) pnpm test
6) pnpm build

COMPLETION RULES
- Keep changes scoped to P3-52 and P3-53 only.
- If a spec detail conflicts with current repo patterns, choose the safer repo-consistent implementation and explain the deviation briefly.
- Commit with a clear conventional commit message referencing P3-52/P3-53.
- End your response with exactly: <promise>ALL TESTS PASSING</promise>
