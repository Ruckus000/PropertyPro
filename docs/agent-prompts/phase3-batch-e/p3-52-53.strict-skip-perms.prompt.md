You are implementing Phase 3 Batch E in PropertyPro with permission prompts disabled.

READ THIS FIRST:
- /Users/jphilistin/Documents/Coding/PropertyPro/docs/agent-prompts/phase3-batch-e/p3-52-53.prompt.md

This file is a STRICT EXECUTION OVERRIDE for running in skip-permissions mode.
Follow the base prompt exactly, plus the rules below. If any rule conflicts, this file wins.

RUN MODE (PERMISSIONS DISABLED)
- You are running with permission prompts disabled (skip-permissions mode).
- You must self-enforce scope boundaries and safety constraints.
- Do not assume broad repo mutation is allowed just because permissions are bypassed.

HARD SCOPE LOCK (NON-NEGOTIABLE)
- Implement ONLY P3-52 (Contract & Vendor Tracking) and P3-53 (Audit Trail Viewer).
- Allowed changes are limited to files directly required for:
  - contracts/audit-trail DB schema + migrations
  - contracts/audit-trail API routes
  - contracts/audit-trail pages/components
  - contracts/audit-trail services/helpers (including CSV export/sanitization)
  - tests for the above
  - strictly required shared exports/barrel files and filter re-exports
- Do NOT modify unrelated features (maintenance, PM portfolio, onboarding, signup, billing, mobile pages outside Batch E needs).
- Do NOT edit docs/plans/AGENTS/CLAUDE/automation files.
- Do NOT change CI/workflows/package versions/tooling config unless absolutely required for Batch E compile/test correctness.
- Do NOT run destructive commands (`git reset --hard`, `git checkout --`, mass deletes, schema drops).

UNRELATED-CHANGE GUARD
- Before each commit attempt and before final response, run:
  - `git diff --name-only`
- If any changed file is outside Batch E scope:
  - stop,
  - explicitly list the out-of-scope files,
  - revert ONLY your accidental out-of-scope changes,
  - continue.
- Do not touch user-authored unrelated changes.

STRICT ITERATION LIMIT (HARD CAP)
- You have a HARD CAP of 8 iterations total. Do not exceed it.
- Do not ask for more iterations.
- Do not restart the loop to evade the cap without human approval.
- If not converged by iteration 8, stop and report:
  - what is complete,
  - what is failing,
  - exact next fix,
  - files touched.

ITERATION DISCIPLINE
- Iteration 1: schema/API shape + failing tests plan (no broad UI work yet).
- Iteration 2-5: implement vertical slices with targeted tests after each slice.
- Iteration 6: close security/access-control gaps and CSV sanitization edge cases.
- Iteration 7: full test pass + build/typecheck/lint checks relevant to scope.
- Iteration 8: only final fixes for failing checks; no new feature expansion.
- If a design issue appears that would require broader refactor, choose the smallest safe Batch E-scoped approach and document the trade-off.

SAFETY + SCOPE ENFORCEMENT RULES
- No speculative refactors.
- No renames/moves outside Batch E surfaces unless required to compile.
- No “while I’m here” cleanup.
- No edits to `.env*` files, secrets, or deployment credentials.
- No changes outside repo root.
- No networked side effects beyond normal package/test/doc lookups needed for implementation.

BATCH E CRITICAL GATES (MUST PASS)
- Contract features gated off for apartment communities (`features.hasCompliance`, not direct type checks).
- Bid embargo enforced server-side until close date.
- Audit CSV export sanitizes formula-injection vectors.

FAIL-FAST CONDITIONS (STOP AND REPORT)
- Migration metadata drift you cannot reconcile safely within Batch E scope.
- You need a new cross-cutting abstraction unrelated to contracts/audit-trail.
- Failing tests indicate regression in unrelated areas not caused by your changes.
- You detect unexpected user changes in files you were about to edit.

FINAL RESPONSE RULES
- If all required checks pass, end with exactly: <promise>ALL TESTS PASSING</promise>
- If anything is incomplete or failing, DO NOT use the success marker.
- Include a concise scope audit summary listing touched files grouped by:
  - schema/migrations
  - API/services
  - UI
  - tests
