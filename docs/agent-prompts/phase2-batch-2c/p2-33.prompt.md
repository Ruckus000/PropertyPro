You are implementing P2-33 (Self-Service Signup) in PropertyPro.

Read these files first, in order:
1) AGENTS.md
2) IMPLEMENTATION_PLAN.md (Task: P2-33)
3) specs/phase-2-multi-tenancy/33-self-service-signup.md
4) PHASE2_EXECUTION_PLAN.md (current cursor + invariants)

Scope and contracts you must satisfy:
- Canonical route for signup UI is /signup.
- Collect required fields: primary contact name, email, password, community name, address, county, unit count, community type (condo_718 | hoa_720 | apartment), plan selection, Terms acceptance.
- Community type changes visible plan/pricing options.
- Subdomain is auto-suggested from community name (normalized lowercase/hyphenated slug).
- Subdomain availability check is debounced UX check plus authoritative server re-check on submit.
- Reserved subdomains are rejected: admin, api, www, mobile, pm, app, dashboard, login, signup, legal.
- Email uniqueness must be non-enumerating.
- Create/link Supabase Auth account with idempotent repeated-submit behavior.
- Send verification through the controlled email flow (Resend-backed), not the default generic path.
- Persist pending signup payload including signupRequestId for checkout handoff.
- Enforce verification-before-checkout handoff.
- Critical lifecycle boundary: do NOT create communities or user_roles in P2-33. Provisioning belongs to P2-35.

Implementation expectations:
- Follow the files expected by the spec:
  - apps/web/src/app/(auth)/signup/page.tsx
  - apps/web/src/components/signup/signup-form.tsx
  - apps/web/src/components/signup/community-type-selector.tsx
  - apps/web/src/components/signup/subdomain-checker.tsx
  - apps/web/src/lib/auth/signup.ts
  - apps/web/src/lib/auth/signup-schema.ts
  - apps/web/src/app/api/v1/auth/signup/route.ts
- If persistence requires DB additions (for pending signup payload), add schema + migration + tests and keep Drizzle migration metadata in sync.
- Every API route must use withErrorHandler.
- Every compliance-relevant mutation must call logAuditEvent.
- No any and no @ts-ignore.

Testing requirements (must be present and passing):
- Validation: required fields, Terms required, weak password, invalid email, invalid unit count.
- Plan/pricing changes by community type.
- Subdomain behavior: reserved rejected, taken rejected, free accepted.
- Server-authoritative submit re-check for subdomain race windows.
- Idempotency: duplicate submit for same signup identity/request does not create duplicate pending signup payload.
- Boundary: signup completion before payment creates no communities/user_roles.

Verification commands:
1) pnpm --filter @propertypro/web typecheck
2) pnpm exec vitest run apps/web/__tests__/signup
3) pnpm test
4) pnpm build

Completion rules:
- Keep changes scoped to P2-33.
- Commit with a clear conventional commit message referencing P2-33.
- End your response with exactly: <promise>ALL TESTS PASSING</promise>
