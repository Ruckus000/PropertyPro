# Phase 5 WS 70 Audit Evidence (2026-03-09)

## 1. Context

- Commit at handoff: `65860c9`
- Branch: `codex/phase-5-table-stakes`
- Date: `2026-03-09`
- Runner: Codex (GPT-5) in local workspace

## 2. Pre-Checks

- Continued from existing dirty working tree on `codex/phase-5-table-stakes` with no reset/revert of unrelated files.
- Loaded runtime env from `.env.local` using:

```bash
set +u; set -a; source .env.local; set +a
```

- Applied migrations before verification:

```bash
pnpm --filter @propertypro/db db:migrate
```

Result: passed; `0081_calendar_sync_and_accounting_connectors` and `0086_package_and_visitor_logging` applied successfully.

## 3. Static Checks

Commands executed:

```bash
pnpm --filter @propertypro/shared build && pnpm --filter @propertypro/db build
pnpm lint
pnpm typecheck
```

Results:

- Shared+DB builds: passed.
- `pnpm lint`: passed (including `guard:db-access`).
- `pnpm typecheck`: failed with pre-existing compliance/react-query issues in `apps/web`:
  - missing module `@tanstack/react-query`
  - implicit `any` errors in existing compliance components/hooks.

## 4. Integration Test Results

Commands executed:

```bash
pnpm test
npx tsx scripts/verify-migration-ordering.ts
npx tsx scripts/verify-no-mocks-in-integration.ts
pnpm exec vitest run --config apps/web/vitest.integration.config.ts apps/web/__tests__/integration/calendar-accounting-connectors.integration.test.ts apps/web/__tests__/integration/package-visitor-logging.integration.test.ts
```

Results:

- `pnpm test`: failed.
  - pre-existing import failure: `apps/web/__tests__/compliance/compliance-filters.test.ts` (`@tanstack/react-query` resolution).
  - additional existing timeout-based failures in legacy UI/accessibility/security unit suites.
- `verify-migration-ordering.ts`: passed (`0` warnings, `0` errors).
- `verify-no-mocks-in-integration.ts`: passed for non-allowlisted files (`31` scanned; `18` clean non-allowlisted).
- WS70 targeted integration: passed.
  - `apps/web/__tests__/integration/calendar-accounting-connectors.integration.test.ts` (`3/3`).

## 5. Cross-Tenant Isolation Verification

Verified by WS70 integration suite:

- `apps/web/__tests__/integration/calendar-accounting-connectors.integration.test.ts`

Coverage includes:

- `GET /api/v1/calendar/my-meetings.ics` rejects cross-tenant actor access (`403`).
- Google connect route rejects cross-tenant actor access (`403`).
- Accounting mapping route rejects cross-tenant actor access (`403`).
- Public ICS feed remains community-scoped (community A feed excludes community B meetings).

## 6. Security Gate Verification

- All WS70 routes are wrapped with `withErrorHandler`.
- Tenant queries use `createScopedClient()` only.
- OAuth tokens are encrypted at application layer via AES-256-GCM (`TOKEN_ENCRYPTION_KEY`) before storage.
- New tenant tables (`calendar_sync_tokens`, `accounting_connections`) have:
  - RLS enabled + force RLS
  - tenant scope trigger (`pp_rls_enforce_tenant_scope`)
  - explicit policies (`pp_tenant_select`, `pp_<table>_insert/update/delete`).
- WS70 mutation paths emit audit logs (`calendar_sync_token`, `accounting_connection`).

## 7. Workstream-Specific Evidence

- DB + crypto:
  - `packages/db/migrations/0081_calendar_sync_and_accounting_connectors.sql`
  - `packages/db/src/crypto/token-encryption.ts`
  - `packages/db/__tests__/token-encryption.test.ts`
  - `packages/db/src/schema/calendar-sync-tokens.ts`
  - `packages/db/src/schema/accounting-connections.ts`
- API/services:
  - `apps/web/src/app/api/v1/calendar/meetings.ics/route.ts`
  - `apps/web/src/app/api/v1/calendar/my-meetings.ics/route.ts`
  - `apps/web/src/app/api/v1/calendar/google/connect/route.ts`
  - `apps/web/src/app/api/v1/calendar/google/callback/route.ts`
  - `apps/web/src/app/api/v1/calendar/google/sync/route.ts`
  - `apps/web/src/app/api/v1/calendar/google/disconnect/route.ts`
  - `apps/web/src/app/api/v1/accounting/connect/route.ts`
  - `apps/web/src/app/api/v1/accounting/callback/route.ts`
  - `apps/web/src/app/api/v1/accounting/export/route.ts`
  - `apps/web/src/app/api/v1/accounting/mapping/route.ts`
  - `apps/web/src/app/api/v1/accounting/disconnect/route.ts`
  - `apps/web/src/lib/services/calendar-sync-service.ts`
  - `apps/web/src/lib/services/accounting-connectors-service.ts`
  - `apps/web/src/lib/accounting/adapters.ts`
  - `apps/web/src/lib/calendar/google-calendar-adapter.ts`
  - `apps/web/src/lib/calendar/ics.ts`
  - `apps/web/src/lib/calendar/common.ts`
  - `apps/web/src/lib/accounting/common.ts`
  - `apps/web/src/lib/calendar/request.ts`
  - `apps/web/src/middleware.ts` (public ICS token-auth allowlist)
- WS70 tests:
  - `apps/web/__tests__/integration/calendar-accounting-connectors.integration.test.ts`
  - `apps/web/__tests__/calendar/calendar-accounting-contract.test.ts` (env-gated contract scaffolding)

## 8. Failure Classification (Required)

### Pre-existing failures

- `pnpm typecheck` failures in existing compliance/react-query surfaces (`@tanstack/react-query` resolution + implicit `any`).
- `pnpm test` failures from:
  - the same known compliance/react-query import issue, and
  - pre-existing timeout-prone legacy suites unrelated to WS70.

### Introduced failures

- None remaining. A transient TS type issue in the new token crypto module was fixed before final verification.
