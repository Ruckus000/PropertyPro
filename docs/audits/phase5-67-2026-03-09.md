# Phase 5 WS 67 Audit Evidence (2026-03-09)

## 1. Context

- Commit at handoff: `65860c9`
- Branch: `codex/phase-5-table-stakes`
- Date: `2026-03-09`
- Runner: Codex (GPT-5) in local workspace

## 2. Pre-Checks

- Continued from existing dirty working tree on `codex/phase-5-table-stakes` (no resets/reverts).
- Loaded runtime env from `.env.local` for networked DB runs via:

```bash
set +u; set -a; source .env.local; set +a
```

- Rebuilt workspace packages to avoid stale `dist` behavior before integration evidence capture:

```bash
pnpm --filter @propertypro/shared build && pnpm --filter @propertypro/db build
```

Result: passed (both packages rebuilt successfully).

- Applied migrations against shared DB before WS67 integration rerun:

```bash
pnpm --filter @propertypro/db db:migrate
```

Result: passed (`migrations applied successfully`).

## 3. Static Checks

Commands executed:

```bash
pnpm lint
pnpm typecheck
```

Results:

- `pnpm lint`: passed (includes `guard:db-access` and migration/RLS checks).
- `pnpm typecheck`: failed with pre-existing compliance/react-query issues unrelated to WS67:
  - missing module `@tanstack/react-query`
  - implicit `any` errors in compliance components/hooks

## 4. Integration Test Results

Commands executed:

```bash
npx tsx scripts/verify-migration-ordering.ts
npx tsx scripts/verify-no-mocks-in-integration.ts
pnpm exec vitest run --config apps/web/vitest.integration.config.ts apps/web/__tests__/integration/violations-arc.integration.test.ts
pnpm test
```

Results:

- `verify-migration-ordering.ts`: passed (`0` warnings, `0` errors; journal last idx/tag `56 / 0056_create_violations_arc`).
- `verify-no-mocks-in-integration.ts`: passed for non-allowlisted files (`14` clean / `27` scanned total); legacy allowlist remains `13` files.
- WS67 real-DB integration rerun:
  - `apps/web/__tests__/integration/violations-arc.integration.test.ts`: passed (`3/3` tests)
  - covered violation lifecycle, ARC lifecycle (including withdraw conflict behavior), and cross-tenant + apartment feature-gating checks.
- `pnpm test`: failed with one pre-existing suite failure:
  - `apps/web/__tests__/compliance/compliance-filters.test.ts`
  - import resolution error for `@tanstack/react-query` from compliance components.

## 5. Cross-Tenant Isolation Verification

Verified by WS67 integration suite:

- `apps/web/__tests__/integration/violations-arc.integration.test.ts`

Coverage includes:

- Table isolation checks:
  - `violations`
  - `violation_fines`
  - `arc_submissions`
- Route-level cross-tenant denial checks for violation and ARC list/detail/mutation surfaces.
- Community-type gating checks (apartment communities blocked from condo/HOA-only WS67 features).

## 6. Security Gate Verification

- WS67 routes are wrapped with `withErrorHandler`.
- Tenant reads/writes use `createScopedClient()` with `community_id` scoping.
- WS67 mutation paths emit audit events through service-layer logging.
- Fine posting writes through WS65 ledger contract (`postLedgerEntry`) and WS66 one-off line-item integration path.
- Fine payment webhook linkage updates `violation_fines.status` idempotently when matching payment metadata is present.

## 7. Workstream-Specific Evidence

WS67 implementation evidence currently in-tree includes:

- Migration and schema:
  - `packages/db/migrations/0056_create_violations_arc.sql`
  - `packages/db/src/schema/violations.ts`
  - `packages/db/src/schema/violation-fines.ts`
  - `packages/db/src/schema/arc-submissions.ts`
  - `packages/db/src/schema/index.ts`
  - `packages/db/src/schema/rls-config.ts`
- WS67 services and routes:
  - `apps/web/src/lib/services/violations-service.ts`
  - `apps/web/src/app/api/v1/violations/route.ts`
  - `apps/web/src/app/api/v1/violations/[id]/route.ts`
  - `apps/web/src/app/api/v1/violations/[id]/fine/route.ts`
  - `apps/web/src/app/api/v1/violations/[id]/resolve/route.ts`
  - `apps/web/src/app/api/v1/violations/[id]/dismiss/route.ts`
  - `apps/web/src/app/api/v1/arc/route.ts`
  - `apps/web/src/app/api/v1/arc/[id]/route.ts`
  - `apps/web/src/app/api/v1/arc/[id]/review/route.ts`
  - `apps/web/src/app/api/v1/arc/[id]/decide/route.ts`
  - `apps/web/src/app/api/v1/arc/[id]/withdraw/route.ts`
- WS66 integration point for fine-payment settlement:
  - `apps/web/src/lib/services/finance-service.ts`
  - `apps/web/src/app/api/v1/webhooks/stripe/route.ts`
- WS67 integration test coverage:
  - `apps/web/__tests__/integration/violations-arc.integration.test.ts`

## 8. Failure Classification (Required)

### Pre-existing failures

- `pnpm typecheck` failure in compliance UI/hooks due missing `@tanstack/react-query` and implicit `any` errors.
- `pnpm test` single failing suite:
  - `apps/web/__tests__/compliance/compliance-filters.test.ts`
  - same `@tanstack/react-query` resolution issue.

### Introduced by WS67 pass

- None detected in this rerun.
